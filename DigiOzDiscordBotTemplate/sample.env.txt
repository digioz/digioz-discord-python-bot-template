# Environment variables for the Discord bot
# Replace placeholder values before running. Do NOT commit real secrets to source control.

# Discord bot token (required)
DISCORD_TOKEN=YOUR_DISCORD_BOT_TOKEN_HERE

# Whether to request the privileged Message Content intent (true/false)
MESSAGE_CONTENT_INTENT=false

# Optional: set to a numeric guild ID for fast slash command registration during development
# Example: GUILD_ID=123456789012345678
GUILD_ID=

# Database connection settings
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=YOUR_DB_PASSWORD
DB_NAME=discord_bot_db_1
